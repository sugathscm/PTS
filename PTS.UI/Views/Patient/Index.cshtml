@model PTS.DAL.Patient
@{
    ViewBag.Title = "Patient";
}

<script type="text/javascript">
    $(document).ready(function () {
        $('#List').DataTable({
            "paging": true,
            "ajax": {
                "url": "/Patient/GetList",
                "type": "POST",
                "datatype": "json"
            },
            "columns": [
                { "data": "Name" },
                //{
                //    "data": "Id", "searchable": false, "ordering": false,
                //    "render": function (data, type, full, meta) {
                //        return '<a class="btn btn-primary btn-xs" href="/Patient/Details/' + data + '">Edit</a>';
                //    }
                //},
                //{
                //    "data": "Id", "searchable": false, "ordering": false,
                //    "render": function (data, type, full, meta) {
                //        return '<a class="btn btn-primary btn-xs" href="/Patient/Report/' + data + '">Report</a>';
                //    }
                //},
            ]
        });
    });
</script>

<div class="dashboard-ecommerce">
    <div class="container-fluid dashboard-content ">
        <!-- ============================================================== -->
        <!-- pageheader  -->
        <!-- ============================================================== -->
        @Html.Partial("_PageHeaderPartial")
        <!-- ============================================================== -->
        <!-- end pageheader  -->
        <!-- ============================================================== -->
        <div class="border border-primary bg-actual-white">
            <div class="row col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12 m-b-20">
                <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12 m-t-20">
                    <div class="table-responsive">
                        <table id="List" class="table table-striped table-bordered first">
                            <thead>
                                <tr>
                                    <th style="width:100%">Name</th>
                                    @*<th></th>*@
                                </tr>
                            </thead>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

